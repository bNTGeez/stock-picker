Run the backend from the repository root:

python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000

Run tests from the repository root:

python -m pytest

The backend package uses repo-root imports, so commands should be launched from
the repository root unless PYTHONPATH is configured explicitly.
